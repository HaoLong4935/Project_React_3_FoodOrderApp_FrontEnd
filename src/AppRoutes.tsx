import { Routes, Route } from "react-router-dom"

export const AppRoutes = () => {
    return (
        <Routes>
            <Route path="/cac" element={<span>Hello</span>} />
        </Routes>
    )
}